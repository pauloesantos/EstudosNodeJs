<script src="/socket.oi/socket.io.js"></script>
<script>
	var socket = io.connect('http://localhost:3000');
	socket.on('new-message', function (data) {
		var chat = document.getElementById('chat_' + data.email);
		chat.href += '?' + data.sala;	
	});
</script>